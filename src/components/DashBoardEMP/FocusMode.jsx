import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function FocusMode() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Focus Mode</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Focus Mode component will be implemented here.</p>
      </CardContent>
    </Card>
  )
}